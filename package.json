{
  "name": "Overlooked History",
  "private": true,
  "version": "1.0.0",
  "author": "Rian Watson",
  "scripts": {
    "serve": "npx @11ty/eleventy --serve --input=src --output=dist --formats=html,njk,gif,png,svg,jpg,jpeg,webp,css,js,md",
    "build": "npx @11ty/eleventy --input=src --output=dist --formats=html,njk,gif,png,svg,jpg,jpeg,webp,css,js,md"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  },
  "dependencies": {
    "@11ty/eleventy-img": "^3.1.0",
    "@11ty/eleventy-plugin-webc": "^0.11.1"
  }
}
